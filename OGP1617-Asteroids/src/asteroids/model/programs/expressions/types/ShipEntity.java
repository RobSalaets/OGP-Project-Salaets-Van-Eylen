package asteroids.model.programs.expressions.types;

import asteroids.model.Ship;

public class ShipEntity extends EntityLiteral {

	public ShipEntity(Ship entity) {
		super(entity);
	}

}
